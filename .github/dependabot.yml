version: 2
updates:
  # Keep GitHub Actions up to date (checkout, configure-pages, etc.)
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly

  # Keep PaperMod submodule up to date
  - package-ecosystem: gitsubmodule
    directory: /
    schedule:
      interval: monthly
